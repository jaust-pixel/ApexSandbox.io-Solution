public Boolean isPrime(Integer num) {
    //code here
    for(Integer check = 2; check < Math.floor(Math.pow(num,0.5)); check++) {
        if(Math.mod(num, check) == 0) {
            return false;
        }
    }
    return true;
}